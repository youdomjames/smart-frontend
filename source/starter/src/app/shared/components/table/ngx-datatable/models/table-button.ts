export class TableButton{
    title:string;
    icon:string;
    action:string;

    constructor(title:string, icon:string, action:string){
        this.title = title;
        this.icon = icon;
        this.action = action;
    }
}